[project]
name = "jobscout-ingest"
version = "0.1.0"
dependencies = [
    "playwright>=1.41",
    "beautifulsoup4>=4.12",
    "requests>=2.31",
    "jobscout-core",
    "jobscout-db"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"